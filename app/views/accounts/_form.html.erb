<%= form_with(model: [@user, account], local: true) do |form| %>
  <% if account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= form.label :acctype %>
      <% @account_types = [account[:acctype]] if account[:acctype] %>
      <%= form.select(:acctype, @account_types, {:include_blank => 'Select an Account Type'},:required => true) %>
    </div>

  <div class="btn waves-effect waves-light" type="submit" name="action">
    <%= form.submit %>
  </div>
<% end %>
